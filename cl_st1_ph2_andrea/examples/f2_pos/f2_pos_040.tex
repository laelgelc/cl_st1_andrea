\begin{textsample}{POS Dim 2 – 1990 – Score 1.00 – 1990/tv\_com\_1990\_81.txt}  \label{ex:f2_pos_040}
Three years ago, we \textbf{set} out to create a sports sedan.
But first and foremost, we \textbf{set} out to create a Lexus.
Introducing the new Lexus ES 300 sports sedan.

% matched lemmas: set
\end{textsample}
